<div>
  <div class="prendas-view">
    <table class="guardarropa">
      <tr>  <%= render 'atributos_atuendo', show: @atuendos.present? %> 
        <% @atuendos.each do |a| %>
            <%= render 'atuendo', atuendo: a %>
        <%end%>
         
    </table>
  </div>
  <div class= "prendas-btn">
      <%= link_to "Crear Atuendo", new_guardarropa_atuendo_path(:guardarropa_id => @guardarropa.id)%>
  </div>
  </tr>
</div>